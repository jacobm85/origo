{
  "root": "//nas/laserdata/skog",
  "filenamePattern": "{id}.laz",

  "port": 3001,
  "corsOrigin": "*",
  "maxBytes": 53687091200,
  "maxCells": 200,

  "_comment_manifest": "Sätt 'manifest': './manifest.json' om filerna har spridda paths/namn — då används den istället för root + filenamePattern.",
  "_comment_docker": "I docker-compose körs backenden helt via env-variabler (LASERDATA_ROOT m.fl.), så config.json behövs inte där."
}
